import { CustomimagePipe } from './customimage.pipe';

describe('CustomimagePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomimagePipe();
    expect(pipe).toBeTruthy();
  });
});
