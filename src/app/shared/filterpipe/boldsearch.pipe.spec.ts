import { BoldsearchPipe } from './boldsearch.pipe';

describe('BoldsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
